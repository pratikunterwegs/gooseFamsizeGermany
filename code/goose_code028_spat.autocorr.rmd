---
title: Checking spatial autocorrelation
---

# Load environment
Only works when gams have already been run. See code026_gamms.

```{r load_libs}
#'male fams.expand spatial
library(sp);library(gstat)
```

```{r check_spa_autocorr}
#'use variogram from gstat. specify directions EWNS, seq(0,270, 90)
#'get df of coords and residuals

#'subset fams.expand
fams.expand.sub = fams.expand[complete.cases(fams.expand[,c("Food_type","Observer","Breeding_year")]),]

mod3resids = as.data.frame(cbind(fams.expand.sub[,c("lon","lat")], resid = resid(mod03)))

#'make spatial
coordinates(mod3resids) = ~ lon+lat
```

## Variogram from ```gstat```

```{r svargram}
#'get semivariogram
vgram_gstat_mod03 = variogram(resid~1, data=mod3resids, alpha= c(90))

```

```{r plot}
#plot semivariogram
plot(vgram_gstat_mod03, smooth = T)
```

## Variogram from ```nlme```

```{r nlme_vgram}
#'load lib
library(nlme)
vgram_nlme_mod03 = Variogram(mod03, form = ~lon)
plot(vgram_gstat_mod06)
```

# For ```mod07```

```{r vgram_mod07}
#'first using gstat
#'specify directions EWNS
mod7resids = as.data.frame(cbind(fams.expand[,c("lon","lat")], resid = resid(mod07$lme)))

#'make spatial
coordinates(mod7resids) = ~ lon+lat

#vgram_gstat_mod07 = variogram(resid~1, data=mod7resids, alpha= c(0,90,180,270))

plot(vgram_gstat_mod07)
plot(vgram_nlme_mod06)
```


